# 배너 이미지 변경 가이드

## 📍 전체 경로 구조

```
yeonyeonghoe-website/
├── public/
│   └── images/
│       └── banner/          ← 여기에 배너 이미지 파일을 넣으세요!
│           ├── banner1.jpg
│           ├── banner2.jpg
│           └── banner3.jpg
└── src/
    └── config/
        └── banner.json       ← 여기서 이미지 경로를 설정합니다
```

---

## 🎯 단계별 가이드

### 1단계: 이미지 파일 준비

배너로 사용할 이미지 파일을 준비하세요.
- 권장 크기: 1920x1080px 이상 (가로형)
- 파일 형식: JPG, PNG, WebP
- 파일 이름: 원하는 이름으로 저장 (예: `my-banner.jpg`)

### 2단계: 이미지 파일을 폴더에 넣기

1. 파일 탐색기에서 다음 경로로 이동:
   ```
   연영회 홈페이지\yeonyeonghoe-website\public\images\banner
   ```

2. 준비한 이미지 파일을 이 폴더에 복사하거나 이동하세요.

   예시:
   - `my-banner1.jpg` 파일을 `banner` 폴더에 넣기
   - `my-banner2.jpg` 파일을 `banner` 폴더에 넣기

### 3단계: banner.json 파일 수정하기

1. 파일 탐색기에서 다음 파일을 찾으세요:
   ```
   연영회 홈페이지\yeonyeonghoe-website\src\config\banner.json
   ```

2. 이 파일을 메모장이나 VS Code 같은 텍스트 에디터로 엽니다.

3. 파일 내용을 보면 다음과 같이 되어 있습니다:
   ```json
   [
     "/images/banner/banner1.jpg",
     "/images/banner/banner2.jpg",
     "/images/banner/banner3.jpg"
   ]
   ```

4. **배너 이미지를 추가하는 경우:**
   
   예를 들어 `my-banner1.jpg` 파일을 추가하고 싶다면:
   ```json
   [
     "/images/banner/banner1.jpg",
     "/images/banner/banner2.jpg",
     "/images/banner/banner3.jpg",
     "/images/banner/my-banner1.jpg"
   ]
   ```

5. **기존 배너 이미지를 교체하는 경우:**
   
   예를 들어 첫 번째 배너를 `my-banner1.jpg`로 바꾸고 싶다면:
   ```json
   {
     "banner": [
       "/images/banner/my-banner1.jpg",    ← 이 줄을 수정
       "/images/banner/banner2.jpg",
       "/images/banner/banner3.jpg"
     ],
     ...
   }
   ```

6. **모든 배너를 새 이미지로 교체하는 경우:**
   ```json
   {
     "banner": [
       "/images/banner/my-banner1.jpg",    ← 모두 새 이미지로 교체
       "/images/banner/my-banner2.jpg",
       "/images/banner/my-banner3.jpg"
     ],
     ...
   }
   ```

7. 파일을 저장하세요 (Ctrl+S)

### 4단계: 반영 확인

별도의 서버 재시작 없이 브라우저에서 홈페이지를 새로고침(F5)하면 즉시 반영됩니다.

---

## 📝 실제 예시

### 예시 1: 배너 이미지 1개 추가하기

**이미지 파일:**
- 파일명: `summer-photo.jpg`
- 위치: `public/images/banner/summer-photo.jpg`

**banner.json 수정:**
```json
[
  "/images/banner/banner1.jpg",
  "/images/banner/banner2.jpg",
  "/images/banner/banner3.jpg",
  "/images/banner/summer-photo.jpg"
]
```

### 예시 2: 모든 배너를 새 이미지로 교체하기

**이미지 파일들:**
- `banner-main.jpg`
- `banner-sub.jpg`
- `banner-extra.jpg`

**banner.json 수정:**
```json
{
  "banner": [
    "/images/banner/banner-main.jpg",
    "/images/banner/banner-sub.jpg",
    "/images/banner/banner-extra.jpg"
  ],
  ...
}
```

### 예시 3: 배너를 1개만 사용하기

**이미지 파일:**
- `main-banner.jpg`

**banner.json 수정:**
```json
{
  "banner": [
    "/images/banner/main-banner.jpg"
  ],
  ...
}
```

---

## ⚠️ 주의사항

1. **경로 형식**: 반드시 `/images/banner/파일명.확장자` 형식을 사용하세요
   - ✅ 올바른 예: `"/images/banner/my-photo.jpg"`
   - ❌ 잘못된 예: `"images/banner/my-photo.jpg"` (앞에 `/` 없음)
   - ❌ 잘못된 예: `"C:\Users\...\my-photo.jpg"` (절대 경로 사용 안 됨)

2. **따옴표**: 경로는 반드시 큰따옴표(`"`)로 감싸야 합니다

3. **쉼표**: 마지막 항목 뒤에는 쉼표(`,`)를 붙이지 않습니다

4. **파일 확장자**: 실제 파일 확장자와 일치해야 합니다 (`.jpg`, `.png`, `.webp` 등)

5. **JSON 문법**: 쉼표, 대괄호, 중괄호를 정확히 사용해야 합니다

---

## 🔍 확인 방법

1. 이미지 파일이 `public/images/banner/` 폴더에 있는지 확인
2. `banner.json` 파일의 경로가 올바른지 확인
3. 브라우저에서 `http://localhost:3000` 접속하여 확인

---

## 🆘 문제 해결

**Q: 이미지가 보이지 않아요**
- 이미지 파일이 `public/images/banner/` 폴더에 있는지 확인
- 파일 이름과 경로가 정확한지 확인
- 브라우저를 새로고침했는지 확인

**Q: JSON 파일 오류가 발생해요**
- 쉼표, 따옴표, 대괄호가 올바른지 확인
- 온라인 JSON 검증 도구 사용: https://jsonlint.com/

**Q: 이미지가 너무 크거나 작아요**
- 이미지 크기를 조정하세요 (권장: 1920x1080px)
- 이미지 편집 프로그램 사용 (Photoshop, GIMP, 온라인 도구 등)
